/// <reference lib="dom" />

// Ensure DOM types are available
declare global {
  // These declarations ensure TypeScript recognizes browser APIs
  interface Window {
    // Additional properties can be added here
  }
}

export {};
